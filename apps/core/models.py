# core/models.py
# Category → apps.categories
# Event    → apps.events
# Ticket   → apps.events
# This file is intentionally left minimal.
# Add core-only models (e.g. SiteSettings, Announcement) here if needed.
